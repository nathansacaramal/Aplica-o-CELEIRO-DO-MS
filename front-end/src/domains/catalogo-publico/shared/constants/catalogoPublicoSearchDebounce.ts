/**
 * Tempo sem nova digitação antes de enviar `busca` à API do catálogo público.
 * Alinhado ao debounce usado em picks do admin (~350ms).
 */
export const CATALOGO_PUBLICO_SEARCH_DEBOUNCE_MS = 350;
